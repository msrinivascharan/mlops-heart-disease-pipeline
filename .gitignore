# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/

# Data (kept out of git — track with DVC / external storage instead)
data/

# Models / experiment artifacts
mlruns/
mlflow.db
mlartifacts/
*.pkl
*.joblib
# Exception: DO track the served model so the repo is self-contained
# (the Dockerfile's `COPY models/` needs it, even on a fresh clone).
!models/*.pkl

mlruns/
mlartifacts/
__pycache__/
*.pyc
.ipynb_checkpoints/
